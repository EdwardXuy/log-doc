# PR #653 缓存机制说明

> **PR**: [sgl-project/sgl-kernel-npu#653](https://github.com/sgl-project/sgl-kernel-npu/pull/653)
> **主题**: ci(deepep): skip rebuild on cache hit, preserve -a arch flags
> **日期**: 2026-08-06

---

## 一、缓存机制全景

PR #653 涉及两个 workflow 的配合：

```
push_build_cache.yml (缓存预热)          pr-test-deepep-npu.yml (PR 测试)
        │                                          │
   push to main                              pull_request
   (源码变更触发)                                   │
        │                                          │
        ▼                                          ▼
   编译 DeepEP wheel                         读取缓存 key
   (build.sh 完整构建)                       (BUILD_HASH 匹配?)
        │                                          │
        ▼                                  ┌───────┴───────┐
   保存 output/ 到缓存                 命中              未命中
   (GitHub Actions Cache)                │                 │
        │                                ▼                 ▼
        │                          pip install        rm + build.sh
        │                          (安装已缓存 wheel)   (完整编译)
        │                          (< 1 min)          (10-15 min)
        └──────────────────────────┘
              缓存 key 匹配时复用
```

### 1.1 push_build_cache.yml — 缓存预热

**触发条件**: push 到 main 分支，且以下文件有变更：
- `build.sh`、`CMakeLists.txt`
- `csrc/**`、`cmake/**`、`python/**`
- `scripts/prepare_deepep_in_container.sh`、`scripts/npu_ci_install_dependency.sh`

**作用**: 在 main 分支上提前编译 DeepEP wheel，保存到 GitHub Actions Cache。PR 测试时可直接复用。

**矩阵**: 2 种硬件 × 2 个 CANN 版本 = 4 个缓存条目

| 硬件 | CANN 版本 | 构建命令 | 产物 |
|------|-----------|----------|------|
| A3 (910C) | 8.5.0 | `bash build.sh -a deepep` | A3 deepep wheel |
| A3 (910C) | 9.0.0 | `bash build.sh -a deepep` | A3 deepep wheel |
| A2 (910B) | 8.5.0 | `bash build.sh -a deepep2` | A2 deepep2 wheel |
| A2 (910B) | 9.0.0 | `bash build.sh -a deepep2` | A2 deepep2 wheel |

### 1.2 缓存 key 设计

```
sgl-kernel-npu-build-v3-{OS}-{arch}-cann{CANN_VERSION}-{BUILD_HASH}
```

| 维度 | 说明 |
|------|------|
| `v3` | 版本号，用于强制失效旧缓存（v2 有 A2 投毒 bug） |
| `OS` | runner 操作系统 |
| `arch` | 硬件架构（a3 / a2） |
| `CANN_VERSION` | 容器内 CANN 版本（动态读取 version.cfg） |
| `BUILD_HASH` | 源码哈希（csrc + cmake + python + build.sh + CMakeLists.txt + 脚本） |

**key 完全一致时才命中** — 任何一个维度变化都会 cache miss。

---

## 二、缓存命中时省掉的步骤

### 2.1 完整编译流程（cache miss）

`build.sh` 执行以下 5 个步骤，总计 **10-15 分钟**：

| 步骤 | 命令 | 耗时 | 说明 |
|------|------|------|------|
| ① CMake 配置 | `cmake -DSOC_VERSION=Ascend910_9382 ... -B build -S` | 1-2 min | 扫描 Ascend 工具链，定位 ASCConfig.cmake，配置 NPU 架构参数，生成 Makefile |
| ② C++ 模块编译 | `cmake --build build --target install -j 16` | 5-8 min | 16 线程并行编译 `csrc/` 下的 C++ 源码（deep_ep_cpp、kernels 等），生成 `.so` 共享库 |
| ③ NPU 算子编译 | `cd csrc/deepep/ops/ && ./build.sh` | 3-5 min | 用 AscendC 编译器把自定义算子（DispatchLayout、AlltoAll 等）编译成 NPU 二进制，生成 `custom_opp*.run` |
| ④ 算子包安装 | `./custom_opp_*.run --install` | ~1 min | 将算子二进制安装到 Ascend 算子目录 |
| ⑤ Wheel 打包 | `python3 setup.py bdist_wheel` | ~1 min | 把 `.so` 文件打包成 `deep_ep*.whl` |

### 2.2 缓存命中流程（cache hit）

| 步骤 | 命令 | 耗时 | 说明 |
|------|------|------|------|
| ① pip install wheel | `pip install output/deep_ep*.whl` | < 1 min | 安装已构建好的 wheel |
| ② 创建符号链接 | `ln -s deep_ep/deep_ep_cpp*.so` | 瞬间 | 链接 .so 到 Python 包目录 |

### 2.3 对比

| | Cache miss（无缓存） | Cache hit（有缓存） |
|---|---|---|
| CMake 配置 | ✅ 执行 | ❌ 跳过 |
| C++ 编译（16线程） | ✅ 执行 | ❌ 跳过 |
| NPU AscendC 算子编译 | ✅ 执行 | ❌ 跳过 |
| 算子包安装 | ✅ 执行 | ❌ 跳过 |
| Wheel 打包 | ✅ 执行 | ❌ 跳过 |
| pip install | ✅ 执行 | ✅ 执行 |
| 耗时 | **10-15 min** | **< 1 min** |
| 每 PR 节省（4 job × 2 版本） | — | **最多 8 × 14 min ≈ 112 min** |

---

## 三、缓存构建失败的回退机制

### 3.1 自动回退设计

**是的，PR #653 有完善的自动回退机制。** 回退逻辑如下：

```
PR 触发 pr-test-deepep-npu.yml
        │
        ▼
   计算 BUILD_HASH
   尝试恢复缓存
        │
   ┌────┴────┐
   命中      未命中
   │          │
   ▼          ▼
 pip install  rm + build.sh
 (-s 跳过构建)  (完整编译)
   │          │
   ▼          ▼
 安装成功 → 继续测试
```

### 3.2 各失败场景的回退

| 失败场景 | 回退方式 | 影响 |
|----------|----------|------|
| **push_build_cache 构建失败** | GitHub Actions 只在 Job 成功时保存缓存。构建失败 → 无缓存 → PR cache miss → 完整编译 | 无影响，走原始构建路径 |
| **push_build_cache 未触发** | main 无源码变更 → 不预热 → PR cache miss → 完整编译 | 无影响，走原始构建路径 |
| **缓存 key 不匹配** | PR 改了源码 → BUILD_HASH 变化 → cache miss → 完整编译 | 无影响，走原始构建路径 |
| **缓存命中但 wheel 损坏** | `pip install` 失败 → `set -e` 退出 → CI 报错 | CI 失败，但概率极低（GitHub Cache 不可变） |
| **缓存命中且 wheel 完好** | `pip install` 成功 → 跳过构建 → 节省 10-15 min | 正常优化路径 |

### 3.3 关键设计保障

1. **cache-miss 路径 = 原始代码**：PR #653 没有修改 cache-miss 的逻辑，行为与优化前完全一致
2. **`set -e` 错误处理**：`prepare_deepep_in_container.sh` 开头加了 `set -e`，任何步骤失败立即退出，不会静默继续
3. **`pip show` 空值检查**：`pip install` 后检查 `deep-ep` 包是否找到，找不到则 `exit 1`
4. **GitHub Cache 不可变**：同一 key 的缓存只能写入一次，不会被覆盖或篡改
5. **BUILD_HASH 覆盖脚本**：`prepare_deepep_in_container.sh` 和 `npu_ci_install_dependency.sh` 的变更会改变 hash，触发 cache miss + 重新构建

### 3.4 结论

> **缓存构建失败不需要人工干预。** push_build_cache 失败或未运行时，PR 测试会自动走 cache-miss 路径（完整编译），行为与优化前完全一致。唯一的代价是失去了加速，但不会影响测试的正确性。
