# PR #518 CI 失败分析报告（2e39708）

> 日期：2026-08-12
> PR：https://github.com/sgl-project/sgl-kernel-npu/pull/518
> Commit：`2e39708`（最新，CI 结果已可用）
> 上一个 Commit：`2245298`（force-reinstall 修复）
> 分支：EdwardXuy/sgl-kernel-npu:Cover-kernel → main

---

## 一、背景

本报告针对 PR #518 在 commit `2e39708` 上的 CI 实际运行结果进行分析。该 PR 旨在为 sgl-kernel-npu 增加 kernel 测试覆盖，CI 配置经过多轮迭代（`308c781` → `f482b63` → `2245298` → `2e39708`）逐步解决构建、依赖、精度等问题。

`2e39708` 引入两项变通方案：
1. 在 `triton/language/__init__.py` 中为 `tl.parallel` 添加兼容性 shim；
2. 在 `run_kernel_tests.sh` 中设置 `ACL_OP_SELECT_IMPL_MODE=high_precision`。

本报告基于 `2e39708` 实际 CI 运行结果评估上述变通方案的有效性，并给出剩余失败的分类与处理建议。

---

## 二、CI 结果汇总（commit 2e39708）

### PR Test：3 通过 / 4 失败（共 5 个测试失败）

| Job | 结果 | 失败测试 |
|-----|------|----------|
| test-cache-ops | ✅ 通过 | 0 |
| test-speculative-ops | ✅ 通过 | 0 |
| test-fla-ops | ✅ 通过 | 0 |
| test-norm-ops | ❌ 失败 | 1：test_add_rmsnorm_bias |
| test-attention-ops | ❌ 失败 | 2：test_decode_attention、test_split_qkv_rmsnorm_rope |
| test-mamba-ops | ❌ 失败 | 1：test_conv1d_prefill |
| test-fused-ops | ❌ 失败 | 1：test_swiglu_quant |

### Daily CANN 9.0.0：6 个失败（40 个测试中 34 个通过）

1. test_add_rmsnorm_bias
2. test_decode_attention
3. test_split_qkv_rmsnorm_rope
4. test_conv1d_prefill
5. test_swiglu_quant
6. test_catlass_matmul_basic（**flaky** — 仅在 Daily 出现，PR test 未出现）

### Daily CANN 8.5.0：大量失败

根因：triton-ascend 3.2.0 与 CANN 8.5.0 不兼容（缺少关键模块 `triton.language.extra.cann`）。

---

## 三、Daily 与 PR Test 失败数差异分析（6 vs 5）

两个流水线失败数差异完全由 **test_catlass_matmul_basic.py** 这一个 **flaky 精度测试** 造成：

- 该测试使用**随机 shape** 进行精度验证。
- Daily 运行时，随机 shape `(427, 195, 366)` 触发 `AssertionError: 0.0078125 not less than or equal to 0.0005`。
- PR test 运行时，随机 shape 碰巧产生较小误差，测试通过。
- 这**不是系统性失败**，而是 float16 精度波动。

### 测试范围差异

- PR test 运行 **39 个测试**（7 个 group，不含 SMOKE_TESTS）。
- Daily 运行 **40 个测试**（ALL_TESTS 额外包含 `test_hello_world.py`）。
- 1 个测试的范围差异（`test_hello_world.py`）不影响失败数，因为该测试本身通过。

---

## 四、triton-ascend 安装方式说明

triton-ascend 在 `prepare_kernel_tests.sh` 中**单独安装**，并非来自 Docker 镜像：

```bash
pip install triton-ascend==3.2.1 --extra-index-url=https://triton-ascend.osinfra.cn/pypi/simple
```

> Docker 镜像可能预装了 triton-ascend，但我们的脚本会用指定版本覆盖它。因此 triton-ascend 的版本完全由 `prepare_kernel_tests.sh` 控制。

---

## 五、Commit `2e39708` 变通方案有效性分析

### 1. tl.parallel 兼容性 shim —— 部分有效

- **变通前**：`AttributeError: module 'triton.language' has no attribute 'parallel'`
- **变通后**：`RuntimeError: Only range and static_range iterators are currently supported`
- **分析**：shim 成功为 `triton.language` 添加了 `parallel` 属性，`getattr()` 不再失败。然而 triton JIT 编译器在 AST 层校验循环迭代器，仅接受 `range` 和 `static_range` —— 即便 `tl.parallel()` 返回的是 Python `range` 对象，JIT 编译器也不接受。
- **结论**：无法通过 CI shim 修复。需要在源码 `decode_attention.py` 第 533 行将 `tl.parallel()` 替换为 `tl.range()`。

### 2. ACL_OP_SELECT_IMPL_MODE=high_precision —— 无效

- `test_swiglu_quant` 仍然失败：`assert max_diff <= 1`（AssertionError）。
- high_precision 模式对自定义 kernel 的量化精度无影响。

---

## 六、失败原因分类（Daily 9.0.0 共 6 个失败）

### 类别 A：源代码问题 —— tl.parallel 被移除（1 个失败）

- **test_decode_attention.py**：`decode_attention.py:533` 调用 `tl.parallel(0, 2, bind_sub_block=True)`。
- triton 3.5.0 已移除 `tl.parallel`，仅支持 `range` 和 `static_range`。
- CI shim 部分生效（错误从 AttributeError 变为 RuntimeError），但 JIT 编译器仍拒绝。
- **修复**：修改源代码，使用 `tl.range()` 或 `tl.static_range()`。

### 类别 B：测试代码 Bug —— 缺少参数（1 个失败）

- **test_split_qkv_rmsnorm_rope.py**：第 165 行调用 `custom_rope(_q, _k, sin, cos)`。
- 函数定义（第 10 行）要求 `half_rope_dim` 参数为必填。
- **修复**：在第 165 行调用处补充 `half_rope_dim` 参数。

### 类别 C：API 参数不匹配（1 个失败）

- **test_add_rmsnorm_bias.py**：`TypeError: add_rmsnorm_bias() got multiple values for argument 'norm_bias'`。
- **修复**：修改测试代码的参数传递方式。

### 类别 D：PyTorch 2.10.0 严格校验（1 个失败）

- **test_conv1d_prefill.py**：`RuntimeError: !argument.default_value() INTERNAL ASSERT FAILED`。
- torch 2.10.0 对函数 schema 校验更严格。
- **修复**：在算子注册时补充默认值，或退回 torch 2.8.0。

### 类别 E：精度问题（2 个失败）

- **test_swiglu_quant.py**：`assert max_diff <= 1` —— 量化精度超出容差。
- **test_catlass_matmul_basic.py**：`0.0078125 > 0.0005` —— float16 随机 shape 精度波动（**FLAKY**）。
- **修复**：调整容差阈值，或改进算法精度。

### 类别 F：CANN 8.5.0 不兼容（仅 Daily）

- `ModuleNotFoundError: No module named 'triton.language.extra.cann'`。
- triton-ascend 3.2.0 缺少关键模块。
- **修复**：CI 侧无可用方案。

---

## 七、已应用的修复（commit 历史）

### Commit `2245298` —— force-reinstall（有效）

- **问题**：Docker 镜像预装 `sgl_kernel_npu==2026.6.1`（版本号相同），pip 跳过安装，预装的 `.so` 缺少 `BUILD_CATLASS_MODULE` 编译的算子。
- **修复**：`pip install sgl_kernel_npu*.whl --force-reinstall --no-deps`。
- **效果**：修复 3 个失败 —— test_catlass_matmul_basic（系统性部分）、test_gmm_wfp8a16、test_mm_wfp8a16。

### Commit `2e39708` —— tl.parallel shim + high_precision（效果有限）

- **tl.parallel shim**：将错误从 `AttributeError` 变为 `RuntimeError`（有进展但未修复）。
- **high_precision 模式**：对 test_swiglu_quant 无效果。

---

## 八、剩余无法从 CI 侧修复的失败

| # | 测试 | 原因 | 修复方 |
|---|------|------|--------|
| 1 | test_decode_attention | 源代码需将 `tl.parallel` 替换为 `tl.range` | 开发 |
| 2 | test_split_qkv_rmsnorm_rope | 测试代码 Bug（第 165 行缺少 `half_rope_dim` 参数） | 开发 |
| 3 | test_add_rmsnorm_bias | API 参数不匹配 | 开发 |
| 4 | test_conv1d_prefill | torch 2.10.0 严格 schema 校验 | 开发 / CI |
| 5 | test_swiglu_quant | 量化精度问题 | 开发 |
| 6 | test_catlass_matmul_basic | flaky float16 精度（重跑可能通过） | — |

---

## 九、建议

1. **反馈 `decode_attention.py:533` 的 `tl.parallel` 问题给开发者** —— 需在源码层修复，将 `tl.parallel()` 替换为 `tl.range()`。
2. **反馈 `test_split_qkv_rmsnorm_rope.py:165` 缺少参数 Bug 给开发者** —— 在调用 `custom_rope()` 时补充 `half_rope_dim` 参数。
3. **反馈 `test_add_rmsnorm_bias.py` API 不匹配问题给开发者** —— 调整 `add_rmsnorm_bias()` 调用处的参数传递方式。
4. **考虑使用 torch 2.8.0（稳定镜像）** —— 以规避 `test_conv1d_prefill` 的 strict schema 校验问题。
5. **CANN 8.5.0 Daily 处理** —— 在 daily workflow 中为 8.5.0 矩阵项添加 `continue-on-error: true`，避免阻塞 daily 流水线。
6. **test_catlass_matmul_basic 处理** —— 该测试为 flaky 精度测试，重跑可能通过；可考虑固定随机 seed 或调整容差阈值以减少波动。
